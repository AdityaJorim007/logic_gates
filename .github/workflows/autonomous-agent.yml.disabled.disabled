# DISABLED - Use bulletproof-ci.yml instead
# This workflow was causing failures due to OpenAI API issues

# name: Autonomous Content Agent
# on:
#   schedule:
#     - cron: '0 */6 * * *'
#   workflow_dispatch:
# jobs:
#   disabled:
#     runs-on: ubuntu-latest
#     steps:
#     - run: echo "This workflow is disabled. Use bulletproof-ci.yml instead."
